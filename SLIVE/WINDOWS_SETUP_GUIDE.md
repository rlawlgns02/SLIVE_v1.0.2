# 🪟 Windows 환경 설정 가이드

한국어 수어 통역 웹앱을 Windows 환경의 VS Code에서 실행하기 위한 완전한 가이드입니다.

---

## 📋 목차

1. [필수 프로그램 설치](#1-필수-프로그램-설치)
2. [프로젝트 클론 및 설정](#2-프로젝트-클론-및-설정)
3. [데이터셋 준비](#3-데이터셋-준비)
4. [모델 학습](#4-모델-학습)
5. [웹앱 실행](#5-웹앱-실행)
6. [문제 해결](#6-문제-해결)

---

## 1. 필수 프로그램 설치

### 1.1 Python 설치

**Python 3.8 ~ 3.10** 버전을 설치합니다. (3.11+는 일부 라이브러리와 호환 문제가 있을 수 있습니다)

1. [Python 공식 사이트](https://www.python.org/downloads/) 방문
2. **Windows installer (64-bit)** 다운로드
3. 설치 시 **"Add Python to PATH"** 체크 ✅
4. 설치 확인:

```bash
python --version
pip --version
```

### 1.2 Visual Studio Code 설치

1. [VS Code 공식 사이트](https://code.visualstudio.com/) 방문
2. Windows용 설치 프로그램 다운로드 및 설치
3. VS Code 실행 후 확장 프로그램 설치:
   - **Python** (Microsoft)
   - **Pylance** (Microsoft)
   - **Jupyter** (선택사항)

### 1.3 Git 설치 (선택사항)

1. [Git for Windows](https://git-scm.com/download/win) 다운로드
2. 설치 (기본 옵션으로 진행)

---

## 2. 프로젝트 클론 및 설정

### 2.1 프로젝트 다운로드

```bash
# Git이 있는 경우
git clone https://github.com/your-username/SLIVE_v1.0.1.git
cd SLIVE_v1.0.1/SLIVE

# Git이 없는 경우: ZIP 파일 다운로드 후 압축 해제
```

### 2.2 VS Code에서 프로젝트 열기

1. VS Code 실행
2. **File > Open Folder** → `SLIVE_v1.0.1/SLIVE` 폴더 선택
3. 터미널 열기: **Ctrl + `** (백틱)

### 2.3 가상 환경 생성 (권장)

```bash
# 가상 환경 생성
python -m venv venv

# 가상 환경 활성화 (Windows PowerShell)
.\venv\Scripts\Activate.ps1

# 또는 cmd에서
venv\Scripts\activate.bat
```

⚠️ **PowerShell 실행 정책 오류가 발생하는 경우:**

```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

### 2.4 패키지 설치

```bash
# 필수 패키지 설치
pip install -r requirements.txt

# PyTorch 설치 (GPU 지원이 필요한 경우)
# https://pytorch.org/get-started/locally/ 에서 명령어 확인

# CPU 버전 (일반적인 경우)
pip install torch torchvision torchaudio

# GPU 버전 (NVIDIA GPU가 있는 경우)
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
```

설치 확인:

```bash
python -c "import torch; print(torch.__version__)"
python -c "import cv2; print(cv2.__version__)"
python -c "import mediapipe; print(mediapipe.__version__)"
```

---

## 3. 데이터셋 준비

### 3.1 AI Hub 데이터셋 다운로드

1. [AI Hub 한국어 수어 데이터셋](https://www.aihub.or.kr/aihubdata/data/view.do?dataSetSn=103) 접속
2. 회원가입 및 로그인
3. **데이터 신청** → 승인 대기 (1~2일 소요)
4. 승인 후 **다운로드** 버튼 클릭
5. **원천데이터**와 **라벨링데이터** 모두 다운로드

### 3.2 데이터셋 배치

다운로드한 데이터를 다음 경로에 배치합니다:

```
SLIVE/
├── 1_data/
│   └── New_sample/
│       ├── 원천데이터/
│       │   └── REAL/
│       │       └── WORD/
│       │           └── 01/
│       │               ├── NIA_SL_WORD1501_REAL01_D.mp4
│       │               ├── NIA_SL_WORD1501_REAL01_F.mp4
│       │               └── ...
│       └── LabelData/
│           └── REAL/
│               └── WORD/
│                   └── 01_real_word_keypoint/
│                       ├── NIA_SL_WORD1501_REAL01_D/
│                       │   ├── *_keypoints.json
│                       │   └── ...
│                       └── ...
```

### 3.3 데이터 전처리

JSON 키포인트 데이터를 NumPy 배열로 변환합니다:

```bash
cd SLIVE
python 1_data/utils/convert_json_to_sequence.py
```

출력:

```
데이터 전처리 시작...
총 3개의 단어 발견
단어 처리: 100%|████████████| 3/3 [00:05<00:00]
전처리 완료! 결과: 1_data/processed/sequence_data
```

---

## 4. 모델 학습

### 4.1 학습 실행

```bash
cd 4_training
python train_word_model_improved.py
```

### 4.2 학습 과정 모니터링

```
============================================================
한국어 수어 단어 분류 모델 학습 시작
디바이스: cuda / cpu
============================================================

데이터 로딩 중: ../1_data/processed/sequence_data
총 15개 샘플, 3개 단어 로드됨
단어 목록: ['WORD1501', 'WORD1502', 'WORD1503']

학습 데이터: 12개
검증 데이터: 3개

모델 파라미터 수: 423,971

학습 시작! 총 50 에포크
------------------------------------------------------------

[Epoch 1/50]
학습 중: 100%|████████████| 1/1 [00:02<00:00, loss=4.5123, acc=33.33%]
학습 손실: 4.5123 | 학습 정확도: 33.33%
검증 손실: 3.8745 | 검증 정확도: 66.67%
✓ 최고 모델 저장됨: ../5_checkpoints/best_word_model.pth
...
```

### 4.3 학습 결과 확인

- **모델 파일**: `5_checkpoints/best_word_model.pth`
- **학습 곡선**: `logs/training_curve.png`
- **학습 이력**: `logs/training_history.json`

---

## 5. 웹앱 실행

### 5.1 Streamlit 웹앱 시작

```bash
cd 3_app
streamlit run streamlit_app.py
```

### 5.2 웹브라우저 접속

자동으로 브라우저가 열립니다. 또는 수동으로 접속:

```
http://localhost:8501
```

### 5.3 웹앱 사용법

1. **"🎥 시작"** 버튼 클릭
2. 웹캠 권한 허용
3. 수어 동작 시연
4. 오른쪽에 인식 결과 표시
5. **"⏹️ 중지"** 버튼으로 종료

#### 설정 조절 (사이드바)

- **신뢰도 임계값**: 낮추면 더 자주 결과가 나오지만 정확도 감소
- **시퀀스 길이**: 늘리면 정확도 향상, 반응 속도 감소
- **키포인트 표시**: 손 추적 점 시각화

---

## 6. 문제 해결

### ❌ 문제 1: `ModuleNotFoundError: No module named 'cv2'`

**해결:**

```bash
pip install opencv-python
```

### ❌ 문제 2: `DLL load failed while importing cv2`

**해결:**

```bash
# Microsoft Visual C++ 재배포 패키지 설치
# https://support.microsoft.com/ko-kr/help/2977003/

# 또는 opencv-python 재설치
pip uninstall opencv-python
pip install opencv-python
```

### ❌ 문제 3: MediaPipe 설치 오류

**해결:**

```bash
# 특정 버전 설치
pip install mediapipe==0.10.0
```

### ❌ 문제 4: 웹캠이 열리지 않음

**해결:**

1. 다른 프로그램에서 웹캠 사용 중인지 확인 (Zoom, Teams 등)
2. Windows 설정 → 개인정보 → 카메라 → 앱에서 카메라 액세스 허용 ✅
3. 웹캠 드라이버 업데이트

### ❌ 문제 5: PowerShell 실행 정책 오류

**해결:**

```powershell
# 관리자 권한으로 PowerShell 실행
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

### ❌ 문제 6: GPU 사용 시 CUDA 오류

**해결:**

```bash
# NVIDIA GPU 드라이버 최신 버전 설치
# https://www.nvidia.com/Download/index.aspx

# CUDA Toolkit 설치 (선택사항)
# https://developer.nvidia.com/cuda-downloads

# PyTorch CUDA 버전 확인
python -c "import torch; print(torch.cuda.is_available())"
```

### ❌ 문제 7: 모델 로드 실패

**해결:**

```bash
# 체크포인트 파일 확인
dir 5_checkpoints

# 없는 경우 모델 재학습
cd 4_training
python train_word_model_improved.py
```

---

## 📚 추가 리소스

### 유용한 명령어

```bash
# 패키지 목록 확인
pip list

# 패키지 업데이트
pip install --upgrade [패키지명]

# 가상 환경 비활성화
deactivate

# VS Code에서 Python 인터프리터 선택
Ctrl + Shift + P → "Python: Select Interpreter"
```

### 프로젝트 구조

```
SLIVE/
├── 1_data/              # 데이터셋
├── 2_models/            # 모델 정의
├── 3_app/               # 웹앱 (streamlit_app.py)
├── 4_training/          # 학습 스크립트
├── 5_checkpoints/       # 학습된 모델
├── 6_tests/             # 테스트
├── logs/                # 학습 로그
└── requirements.txt     # 패키지 목록
```

---

## 🎯 다음 단계

1. **전체 데이터셋으로 학습**: 샘플 데이터 대신 전체 AI Hub 데이터셋 사용
2. **모델 개선**: 하이퍼파라미터 튜닝, Transformer 모델 시도
3. **문장 단위 번역**: Seq2Seq 모델 구현
4. **모바일 앱 개발**: Flutter, React Native 등으로 포팅
5. **배포**: Heroku, AWS, Azure 등으로 배포

---

## 📞 문의 및 지원

문제가 해결되지 않는 경우:

1. GitHub Issues에 문의
2. 에러 메시지 전체 복사
3. 실행 환경 정보 (Python 버전, OS 버전 등) 포함

**즐거운 코딩 되세요! 🚀**
